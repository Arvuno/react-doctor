{
  "name": "@react-doctor/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: Effect-based runtime layer for React Doctor — Schema-decoded wire formats, tagged-error facades, and a streaming Linter pipeline backed by Context.Services. Not published.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && NODE_ENV=production vp pack",
    "typecheck": "tsc --noEmit",
    "test": "vp test run"
  },
  "dependencies": {
    "@react-doctor/core": "workspace:*",
    "@react-doctor/types": "workspace:*",
    "effect": "4.0.0-beta.69"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
